namespace Tera.Analytics
{
    /// <summary>
    ///     Specifies the codes that indicate the type of an attribute's value.
    /// </summary>
    public enum TypeCode : ushort
    {
        Int = 1,
        Float = 2,
        Bool = 5
    }
}